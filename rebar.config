{erl_opts, [debug_info]}.
{deps, [{cowboy, "2.9.0"}]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [cowboy, dapper]}
]}.

{dist_node, [
    {setcookie, dapper_cookie},
    {sname, dapper}
]}.
